# Android - the next implementation

Do not implement Android before Windows stabilizes and the owner selects the corresponding goal. Initial Android research in the conversation is historical input, not an approved Kotlin or .NET Android stack.

Shared with Windows: product requirements, quota/group/identity semantics, source evidence, sanitized fixtures and safety/acceptance concepts. Re-evaluate native Android UI, authentication redirects, credential storage, lifetime, background execution, widgets and persistence when that work is selected. Do not make Android load WinUI, Windows-specific .NET or DPAPI assemblies.

Backlog owner: AIU-018. Windows Widgets are not Android home-screen widgets.
