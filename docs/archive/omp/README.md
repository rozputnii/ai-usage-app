# Retired OMP workflow archive

Historical, non-executable evidence. These files are excluded from active instructions and current-contract validation. Do not load archived skills, run extensions or use archived tests as CI gates.

The owner-approved agent-neutral migration retired this bridge on 2026-09-14. Baseline commit: `1e4f0ced764132027acc13f7d58d7957d66242be`. Each explicitly archived source is preserved byte-for-byte at `docs/archive/omp/<original repository-relative path>`, including the pre-existing edits to `.omp/AGENTS.md` and `.omp/RULES.md` and the local authored `.codex/config.toml`. No runtime sessions or credentials were copied.

The original OMP entry was copied before its active replacement. Historical tests and relative links describe the old layout; they are not current requirements. Provider research citing OMP remains valid provenance and is not retired by this workflow change.

Use [CONTRIBUTING](../../../CONTRIBUTING.md) for current development. Recovery must restore only explicitly mapped migration-owned files or hunks; never reset the dirty checkout to the baseline commit.
